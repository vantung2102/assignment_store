.page-header
    h1 List of Categories
ol.breadcrumb
    li.dashboard
        a class="pjax" href=""
            |Dashboard
    li.active Categories
.container
    - flash.each do |message_type, message|
        div class="alert alert-dismissible alert-#{message_type}" style="font-size: 15px;"
            button class="close" data-dismiss="alert" type="button" ×
            = message
ul.nav.nav-tabs.action_items
    li.active
        a class="pjax"
            i.fa.fa-list
            span List
    li
        a class="pjax" href="#{new_admin_product_url}"
            i.fa.fa-plus
            span New
    li
        a class="pjax"
            i.fa.fa-share
            span Export
    div class="right" style="float:right"
        li class="dropdown" 
            a class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false"
                i.fa.fa-filter
                |Add filter
                b.caret
            ul class="dropdown-menu" style="left:auto; right:0;"
                li
                    a href="#" Id
.col-lg-12.grid-margin.stretch-card
    .card
        .card-body
            .table-responsive
                table.table.table-striped
                    thead
                        tr
                            th STT
                            th ID
                            th Title
                            th Meta Title
                            th Slug
                            th Price
                            th Discount
                            th Quantity
                            th Brand
                            th Category
                            th Type
                            th Sold
                            th Stock Keeping Unit
                            th Created At
                            th Updated At
                            th Other

                    tbody
                        - i = 0
                        - @data[:products].each do |product|
                            - i += 1
                            tr
                                td = i
                                td = product.id
                                td = product.title
                                td = product.meta_title
                                td = product.slug
                                td = product.created_at
                                td = product.updated_at
                                td style="width: 80px"
                                    ul.others
                                        li
                                            a href="#{admin_product_url(product.id)}" title="view"
                                                i.fa.fa-info-circle
                                        li
                                            a href="#{edit_admin_product_path(product.id)}" title="edit"
                                                i.fa.fa-edit
                                        li
                                            a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href="#{admin_product_url(product.id)}"
                                                i.fa.fa-trash-o